apiVersion: v1
kind: Service
metadata:
  name: udacity-capstone-service
spec:
  type: LoadBalancer
  selector:
    app: udacity-capstone-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
